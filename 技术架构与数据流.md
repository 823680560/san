# 环卫行业智能问答系统 — 技术架构与数据流

## 一、整体架构概览

```text
┌──────────────────────────────────────────────────────────────────┐
│                       Web 层 (FastAPI)                            │
│  ┌────────┐  ┌──────────┐  ┌────────────────────────────────┐   │
│  │ Upload │  │Vectorize │  │  Chat (RAG / Agent)             │   │
│  │ API    │→ │API       │→ │  ├─ POST /api/chat (非流式)     │   │
│  └────────┘  └──────────┘  │  └─ POST /api/chat/stream (SSE) │   │
├──────────────────────────────────────────────────────────────────┤
│                       Agent 层 (LangChain)                        │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │  ReAct Agent (ZERO_SHOT_REACT_DESCRIPTION)                 │  │
│  │  Memory: ConversationBufferWindow (k=5) + _SafeMemory      │  │
│  │  工具: law_search / project_search_db / search_internet    │  │
│  │  回调: _StreamingCallbackHandler (SSE 推送)                │  │
│  └──────┬──────────────┬──────────────┬───────────────────────┘  │
│         │              │              │                          │
│         ▼              ▼              ▼                          │
│  ┌──────────┐  ┌────────────┐  ┌──────────────────┐             │
│  │ 法律 RAG │  │ NL2SQL     │  │ 智谱 Web 搜索 API│             │
│  └──────────┘  └────────────┘  └──────────────────┘             │
├──────────────────────────────────────────────────────────────────┤
│                      模型层                                       │
│  ┌────────────────┐  ┌──────────────┐  ┌──────────────────────┐ │
│  │ BGE-M3 (嵌入)  │  │ BGE-Rerank  │  │ LLM (DeepSeek Chat)  │ │
│  │ HuggingFace    │  │ -v2-m3      │  │ OpenAI 兼容 API      │ │
│  │ 本地/HF下载    │  │ CrossEncoder│  │ api.deepseek.com/v1  │ │
│  └────────────────┘  └──────────────┘  └──────────────────────┘ │
├──────────────────────────────────────────────────────────────────┤
│                      数据层                                       │
│  ┌──────────┐  ┌──────────────┐  ┌────────────────────────────┐ │
│  │ Excel →  │  │ JSON/PDF →   │  │ FAISS 向量索引              │ │
│  │ SQLite   │  │ Document     │  │ knowledge_base/             │ │
│  │ + FTS5   │  │              │  │                             │ │
│  └──────────┘  └──────────────┘  └────────────────────────────┘ │
├──────────────────────────────────────────────────────────────────┤
│                      评估层 (eval/)                               │
│  ┌────────────┐ ┌───────────┐ ┌──────────┐ ┌──────────────────┐ │
│  │ LLM Judge  │ │ 检索评估  │ │ NL2SQL   │ │ 数据集构建        │ │
│  │ 正确性/相关│ │ Recall/   │ │ 评估     │ │ 法律+项目问答对   │ │
│  │ 性/忠实度  │ │ Precision │ │          │ │                  │ │
│  └────────────┘ └───────────┘ └──────────┘ └──────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

---

## 二、数据流详解

### 流程 1：环卫项目数据 — Excel → SQLite → NL2SQL 查询

```
data/project/环卫项目数据.xlsx
        │
        ▼
  server/rag/project_db.py
        │  import_excel_to_sqlite()
        │  ① 读取 Excel (pandas)
        │  ② 列名映射（中文 → 英文列名）
        │  ③ 写入 SQLite 结构表 sanitation_projects（27 个字段）
        │  ④ jieba 分词预处理（multiprocessing 多进程并行）
        │     → 写入 FTS5 全文索引表 sanitation_projects_fts
        │  ⑤ 保存 mtime+size 哈希，避免重复导入
        ▼
  data/project/sanitation_projects.db
        │
        ▼
  server/rag/project_query_engine.py  ←  ProjectQueryEngine
        │
        ├─ 启动时自动导入/更新数据
        │
        ├─ classify() ─── 查询类型分类器
        │   ├─ 规则匹配（关键词如"排名"→aggregation）
        │   └─ LLM fallback（规则无法判断时调用 DeepSeek API）
        │
        ├─ exact / fuzzy ─────────────┐
        │   ▼                         │
        │  SQLite Vanna (NL2SQL)      │
        │   ├─ DDL + 文档 + 222+ 问答对
        │   │  训练数据                │
        │   ├─ jieba Jaccard +        │
        │   │  Embedding 混合检索      │
        │   │  相似示例 (权重 0.3:0.7) │
        │   └─ LLM 生成 SQL           │
        │       │                     │
        │       ▼                     │
        │  SQLGlot 校验(SQLValidator)  │
        │   ├─ 语法解析                │
        │   ├─ 语句白名单(SELECT/WITH)  │
        │   └─ 操作黑名单(INSERT/DROP…) │
        │       │                     │
        │       ▼                     │
        │  MATCH 子句 jieba 分词后处理  │
        │       │                     │
        │       ▼                     │
        │  SQLite 执行 → DataFrame    │
        │                             │
        ├─ aggregation ───────────────┘
        │   ▼
        │  DuckDB Vanna (NL2SQL)
        │   ├─ ATTACH SQLite 数据库
        │   ├─ 支持 strftime 等分析函数
        │   ├─ DDL + 文档 + 16 个聚合问答对训练
        │   └─ LLM 生成 SQL
        │       │
        │       ▼
        │  SQLGlot 校验
        │       │
        │       ▼
        │  DuckDB 执行 → DataFrame
        │
        ▼
  Agent 工具 project_search_db
        │  格式化结果（项目名称/金额/日期等）
        │  过滤 Unicode 非法代理字符
        ▼
  LLM 生成最终回答
```

**关键文件：**
| 文件 | 作用 |
|------|------|
| `server/rag/project_db.py` | Excel 读取、SQLite 建表、FTS5 索引、多进程 jieba 分词导入 |
| `server/rag/project_query_engine.py` | 查询分类 + NL2SQL 路由 + SQL 校验 + MATCH 后处理 + 执行 |
| `server/rag/vanna_setup.py` | Vanna 训练数据（DDL + 文档 + 222+ SQLite 问答对 + 16 DuckDB 问答对） |
| `server/rag/vanna_llm.py` | Vanna LLM 适配器（OpenAI 兼容 API，混合检索相似示例） |
| `server/rag/sql_validator.py` | SQLGlot 安全校验 |
| `server/agent/tools/project_search_db.py` | Agent 工具的封装与结果格式化 |

---

### 流程 2：法律法规数据 — JSON/PDF → FAISS → RAG 检索

```
data/law/
  ├─ 招采政策法规.json    ←  结构化法规（政策标题+内容+时间）
  └─ *.pdf                          ←  扫描件/文档法规
        │
        ▼
  server/rag/data_loader.py
        │  load_json_data()  /  load_law_pdf_data()
        │  JSON：提取 政策标题/内容/发布时间/施行时间/来源网站
        │  PDF：PyMuPDF 逐页提取 → 清洗非法代理字符 → 对齐 JSON 元数据格式
        ▼
  llama_index Document 列表
        │
        ▼
  SentenceSplitter              ← 分片
        │  chunk_size=800, overlap=100
        ▼
  文本节点 (TextNode) 列表
        │
        ▼
  server/rag/vector_store.py
        │  create_faiss_index()
        │  ① BGE-M3 嵌入模型（HuggingFace 本地加载）→ 向量化每个节点
        │  ② FAISS IndexFlatIP (内积索引, 1024 维)
        │  ③ 持久化到 knowledge_base/law/
        │  ④ 启动时 check_load_law_kb() 自动检查/构建
        ▼
  knowledge_base/law/  ← FAISS 索引 + 文档存储

  查询时：
  用户问题
        │
        ▼
  server/agent/tools/law_search.py  ← LawSearchEngine（混合检索逻辑内嵌在此）
        │
        ├─ 向量检索 (VectorIndexRetriever)  top_k=30
        │   │  BGE-M3 编码查询 → FAISS IndexFlatIP 检索
        │   ▼
        ├─ BM25 检索 (JiebaBM25Retriever)   top_k=60
        │   │  jieba 分词 → bm25s 关键词检索
        │   ▼
        ├─ Query Fusion (Reciprocal Rank Fusion)
        │   权重: [BM25=0.2, Vector=0.8], mode="relative_score"
        │   融合取 top_k=60 送入 Reranker
        │   ▼
        ├─ BGE-Reranker-v2-m3 (CrossEncoder)
        │   对 60 个候选逐对(query, doc)打分
        │   重排序 → 输出 top_n=5
        │   ▼
        └─ 缓存 (LRU, key="query::top_k", 相同 query 复用)
              │
              ▼
  Agent 工具 law_search
        │  格式化（含元数据：标题/发布时间/来源/内容）
        │  过滤 Unicode 非法代理字符
        ▼
  LLM 生成最终回答
```

**注：** 架构文档此前引用的 `server/rag/hybrid_retriever.py` 文件不存在。混合检索逻辑实际内嵌在 `server/agent/tools/law_search.py` 的 `LawSearchEngine` 类中（第 24-46 行）。

**关键文件：**
| 文件 | 作用 |
|------|------|
| `server/rag/data_loader.py` | 多格式数据解析（Excel/JSON/PDF/CSV/TXT/DOCX/DOC） |
| `server/rag/vector_store.py` | FAISS 索引构建、加载、持久化（含 PDF 文件批量处理 + 启动自动检查） |
| `server/rag/jieba_bm25.py` | 中文 BM25 检索器（jieba 分词 + bm25s，继承 LlamaIndex BaseRetriever） |
| `server/rag/reranker.py` | BGE-Reranker-v2-m3 CrossEncoder 重排序 |
| `server/agent/tools/law_search.py` | LawSearchEngine 封装（混合检索+重排序+缓存，含 Unicode 代理字符过滤） |
| `server/embeddings/bge_m3.py` | BGE-M3 Ollama 嵌入包装器（当前主代码路径使用 HuggingFace 本地模型） |

---

### 流程 3：文件上传 → 向量化 → 自定义知识库

```
用户上传文件 (Excel/PDF/JSON等)
        │
        ▼
  POST /api/upload  ← upload_api.py
        │  ① 校验扩展名白名单
        │  ② 保存到 data/ 目录（同名自动编号）
        ▼
  data/ 目录中已有文件
        │
        ▼
  POST /api/vectorize  ← vectorize_api.py
        │  { file_name, kb_name, chunk_size, chunk_overlap }
        ▼
  server/rag/vector_store.py :: build_index_from_file()
        │
        ├─ 1. DataLoader.load_file()  → 自动识别扩展名
        │   ├─ .xlsx/.xls → load_excel_data()
        │   ├─ .json      → load_json_data()
        │   ├─ .pdf       → load_pdf_data()
        │   ├─ .csv       → load_csv_data()
        │   ├─ .txt       → load_txt_data()
        │   ├─ .docx      → load_docx_data()
        │   └─ .doc       → load_doc_data()
        │
        ├─ 2. SentenceSplitter 分片
        │
        ├─ 3. BGE-M3 向量化 (HuggingFace)
        │
        └─ 4. FAISS IndexFlatIP → 持久化到 knowledge_base/<kb_name>/
                      │
                      ▼
  知识库列表 GET /api/knowledge-bases
        对话时加载对应索引进行检索
```

**关键文件：**
| 文件 | 作用 |
|------|------|
| `web/api/upload_api.py` | 文件上传接口，白名单校验，防重名 |
| `web/api/vectorize_api.py` | 向量化构建接口，知识库管理 |
| `server/rag/vector_store.py` | 核心索引构建逻辑 |

---

### 流程 4：对话查询 — 非流式 (POST /api/chat)

```
用户提问
        │
        ▼
  POST /api/chat  ← chat_api.py
        │  { query, kb_name, top_k, session_id }
        │
        ├─ kb_name="auto" ─────────────────────── Agent 模式
        │   │
        │   ▼
        │  server/agent/agent_factory.py
        │   │  init_agent(session_id)  ← 线程安全单例
        │   │
        │   ├─ 1. 加载法律知识库 (FAISS index), check_load_law_kb()
        │   │
        │   ├─ 2. 创建三个工具（全局共享）：
        │   │   ├─ law_search(query)         → 法律混合 RAG 检索 + Reranker
        │   │   ├─ project_search_db(query)  → NL2SQL 查询 (SQLite/DuckDB)
        │   │   └─ search_internet(query)    → 智谱 Web 搜索 API (https://open.bigmodel.cn/api/paas/v4/web_search)
        │   │
        │   ├─ 3. LLM (DeepSeek Chat via OpenAI 兼容 API) 作为 ReAct Agent 大脑
        │   │      ChatOpenAI(model="deepseek-chat", base_url="https://api.deepseek.com/v1")
        │   │
        │   ├─ 4. 历史同步：chat_histories[session_id] → seed_agent_memory()
        │   │      将统一历史灌入 Agent 的 ConversationBufferWindowMemory(k=5)
        │   │
        │   ├─ 5. ReAct 循环：
        │   │   Thought → Action → Observation → Thought → ... → Final Answer
        │   │   规则：最多 5 轮迭代，同一方向最多 2 次工具调用，获取足够信息后必须停止
        │   │
        │   ├─ 6. 回写：结果写入 chat_histories[session_id]
        │   │
        │   └─ 7. 返回 { answer, thinking_steps, tool_calls_count }
        │
        ├─ kb_name="law" ──────────────────── 法律单库模式
        │   │
        │   ▼
        │  LawSearchEngine.search(query)
        │   → 混合检索 (Vector+BM25) + Reranker → 结果作为 context
        │   → SimpleLLM (eval/llm_wrapper.py, ChatOpenAI 封装)
        │   → 基于 context + chat_rag_prompt 生成回答
        │
        ├─ kb_name="project" ──────────────── 项目单库模式
        │   │
        │   ▼
        │  ProjectQueryEngine.query(query)
        │   → NL2SQL 生成 + 执行 → 结果格式化作为 context
        │   → SimpleLLM 基于 context 生成回答
        │
        └─ 其他 kb_name ──────────────────── 自定义知识库
            │
            ▼
          通用检索引擎 (GenericSearchEngine)
           → 尝试混合检索 (Vector + BM25)，退化到纯向量检索
           → 结果作为 context → SimpleLLM 生成回答
```

---

### 流程 5：对话查询 — SSE 流式 (POST /api/chat/stream) **[新增]**

```
用户提问（Web 前端 chat.html）
        │
        ▼
  POST /api/chat/stream  ← chat_streaming.py
        │  { query, kb_name, top_k, session_id }
        │  返回: text/event-stream (SSE)
        │
        ├─ kb_name="auto" ──────────────────── Agent 流式模式
        │   │
        │   ▼
        │  _agent_sse_generator()
        │   │
        │   ├─ 1. 创建 _StreamingCallbackHandler (继承 BaseCallbackHandler)
        │   │     内建 queue.Queue，线程安全推送事件
        │   │
        │   ├─ 2. 后台线程执行 run_agent_query_streaming()
        │   │     传入 callbacks=[callback]
        │   │
        │   ├─ 3. 实时 SSE 事件推送：
        │   │   ┌─────────────────────────────────────────────┐
        │   │   │ event: status       → "Agent 正在思考..."    │
        │   │   │ event: thinking_step → {step, tool, tool_input} │
        │   │   │ event: tool_result  → {step, observation_preview} │
        │   │   │ event: token        → {text} (逐 token 推送) │
        │   │   │ event: done         → {status: "complete"}   │
        │   │   │ event: error        → {message}              │
        │   │   └─────────────────────────────────────────────┘
        │   │
        │   ├─ 4. Thought/Token 解析：逐行扫描 LLM 输出
        │   │     "Thought:" → thinking_step 事件
        │   │     "Final Answer:" 之后 → token 事件（逐字流式）
        │   │
        │   └─ 5. 结果回写 chat_histories[session_id]
        │
        └─ kb_name ≠ "auto" ──────────────────── 知识库流式模式
            │
            ▼
          _kb_sse_generator()
            │
            ├─ Phase 1: status=retrieving → 检索知识库
            ├─ Phase 2: context → 发送检索结果给前端展示
            ├─ Phase 3: token → iterate_in_threadpool() 逐 token 流式生成
            └─ Phase 4: done → 结果回写 chat_histories
```

**SSE 事件类型一览：**

| 事件类型 | 触发时机 | 携带数据 |
|---------|---------|---------|
| `status` | 阶段切换 | `{status: "retrieving"\|"generating", message}` |
| `thinking_step` | Agent 调用工具 | `{step, tool, tool_input}` |
| `tool_result` | 工具返回结果 | `{step, observation_preview}` |
| `token` | LLM 逐 token 输出 | `{text}` |
| `context` | 检索完成（非Agent模式） | `{text}` |
| `done` | 生成完成 | `{status: "complete"}` |
| `error` | 任何错误 | `{message}` |

**回调处理器关键机制 (chat_streaming.py:35-127)：**
- `on_llm_new_token()`: 逐行缓冲解析，检测 `Thought:` 和 `Final Answer:` 标记
- `on_agent_action()`: 捕获工具调用（工具名 + 参数）
- `on_tool_end()`: 捕获工具返回结果（截断至 2000 字符）
- "Final Answer:" 之前的 token 不输出给用户，之后才开始逐字流式推送

**关键文件：**
| 文件 | 作用 |
|------|------|
| `web/api/chat_streaming.py` | SSE 流式端点，回调处理器，异步事件循环 |
| `web/api/chat_api.py` | 非流式端点，统一历史管理，检索引擎缓存 |
| `web/templates/chat.html` | 前端 SSE 解析，可折叠思考过程，流式回退到非流式 |
| `eval/llm_wrapper.py` | SimpleLLM 封装 ChatOpenAI，提供 complete() 和 stream() |

---

### 流程 6：对话历史管理 **[新增]**

```
┌──────────────────────────────────────────────────────────────┐
│                    统一历史存储                                │
│  chat_histories: dict[str, list]  ← chat_api.py:44           │
│  {                                                           │
│    "session_id": [                                            │
│      {"role": "user", "content": "..."},                      │
│      {"role": "assistant", "content": "..."},                 │
│      ...                                                     │
│    ]                                                         │
│  }                                                           │
│  所有模式共享：Agent / RAG / 流式 / 非流式                      │
└────────────────────────┬─────────────────────────────────────┘
                         │
          ┌──────────────┼──────────────┐
          ▼              ▼              ▼
  Agent 模式         单库 RAG 模式     流式模式
  (非流式/流式)      
          │              
          ▼
  seed_agent_memory()   ← agent_factory.py:174
    ├─ 先清空 Agent memory
    └─ 按 (user, assistant) 配对逐条灌入 ConversationBufferWindowMemory
          │
          ▼
  ReAct 对话（Agent 内部 memory 保持 k=5 窗口）
          │
          ▼
  结果回写 chat_histories[session_id]
    chat_histories[id].append({"role": "user", "content": query})
    chat_histories[id].append({"role": "assistant", "content": answer})
```

**设计要点：**
- `chat_histories` 是唯一数据源，所有 API 模式统一读写
- Agent 内部 `ConversationBufferWindowMemory` 是工作副本，每次查询前从 `chat_histories` 同步
- 清空操作 (`POST /api/chat/clear`) 同时清空 `chat_histories` 和 Agent memory
- 非 Agent 模式通过 `_format_history()` 将最近 5 轮转为文本注入 prompt

---

## 三、关键模型配置

| 模型 | 用途 | 加载方式 | 维度/说明 |
|------|------|---------|-----------|
| BGE-M3 | 文本嵌入/向量化 | HuggingFace 本地加载 (`models/bge-m3/`)，不存在时自动从 HF 下载 | 1024 |
| BGE-Reranker-v2-m3 | 检索结果重排序 | HuggingFace CrossEncoder 本地加载 (`models/bge-reranker-v2-m3/`) | - |
| DeepSeek Chat | LLM 对话/NL2SQL/Agent | OpenAI 兼容 API (`https://api.deepseek.com/v1`) | temperature=0.7 (Agent) / 0 (RAG) |
| 智谱 Web Search | 互联网搜索工具 | 智谱 API (`https://open.bigmodel.cn/api/paas/v4/web_search`) | 需要 `ZHIPU_API_KEY` |

**配置方式：** 所有密钥和端点从项目根目录 `.env` 文件加载（`configs/config.py` 通过 `python-dotenv` 读取），支持环境变量覆盖。

**嵌入模型加载策略 (`config.py:93-101`)：**
- 优先使用本地路径 (`models/bge-m3/`)
- 本地不存在时返回 HuggingFace 模型 ID (`BAAI/bge-m3`)，触发自动下载
- 下载缓存到 `~/.cache/huggingface/`（Docker 中挂载为 `hf_cache` 卷）
- HuggingFace 镜像端点默认 `https://hf-mirror.com`（可通过 `HF_ENDPOINT` 环境变量配置）

---

## 四、核心数据结构

### SQLite 项目表 (`sanitation_projects`)

27 个字段，涵盖：
- **地理信息**：大区、省份、城市、区县、城乡分类
- **项目信息**：项目名称、项目类型、项目状态、是否PPP
- **参与方**：招标单位、招标代理、采购主体、中标公司
- **财务**：年化金额、合同金额、年限
- **时间**：中标时间、时间节点、预计合同到期时间、剩余合同期限
- **业务**：清扫面积、单价、转运补贴/处理量、转运站个数

### FTS5 全文索引

10 个文本列（jieba 分词预处理，multiprocessing 多进程并行）：
项目名称、招标单位、招标代理、采购主体、项目类型、项目状态、中标公司、城乡分类、备注、是否PPP

### FAISS 向量索引

- 法律知识库：`knowledge_base/law/`，`IndexFlatIP`，1024 维
- 自定义知识库：`knowledge_base/<kb_name>/`

---

## 五、检索漏斗（法律知识库）

```text
原始文档 (JSON + PDF)
   │  PyMuPDF 提取 + Unicode 代理字符清洗
   ▼
SentenceSplitter (800/100)
   │
   ▼
┌─────────────────┐     ┌─────────────────┐
│ 向量检索 top 30  │     │ BM25 检索 top 60 │
│ (BGE-M3 + FAISS │     │ (jieba + bm25s)  │
│  IndexFlatIP)   │     │                  │
└────────┬────────┘     └────────┬─────────┘
         │                       │
         └──────────┬────────────┘
                    │
                    ▼
         Query Fusion (权重 BM25=0.2 + Vector=0.8)
         mode="relative_score"
                    │
                    ▼
             候选集 top 60
                    │
                    ▼
         BGE-Reranker-v2-m3 (CrossEncoder)
                    │
                    ▼
             精排输出 top 5
                    │
                    ▼
              LLM 生成回答
```

---

## 六、NL2SQL 查询漏斗（项目库）

```text
  用户自然语言问题
        │
        ▼
  ┌─────┴──────┐
  │ 查询分类器   │ ← 规则匹配 + LLM fallback (DeepSeek API)
  └─────┬──────┘
        │
  ┌─────┴──────┬──────────┐
  │ exact/fuzzy│aggregation│
  └─────┬──────┴──────────┘
        │
        ▼
  Vanna NL2SQL (LLM 生成 SQL)
        │  ① 混合相似问答对检索 (jieba Jaccard 0.3 + Embedding 0.7) → top 5
        │  ② 相关 DDL + 文档注入
        │  ③ LLM 合成 SQL, 可选中间执行步骤 (execute_sql=True)
        │  ④ 失败时自动回退重试
        ▼
  SQLGlot 校验
        │  (语法+白名单 SELECT/WITH + 黑名单正则)
        ▼
  MATCH 子句 jieba 分词后处理 (确保与 FTS5 入库一致)
        │
        ▼
  执行引擎 (SQLite / DuckDB)
        │
        ▼
  结果格式化 → Agent 返回 → LLM 最终回答
```

---

## 七、Unicode 代理字符过滤 **[新增]**

PyMuPDF 解析 OCR PDF 时可能产生 Unicode 非法代理字符 (`\ud800-\udfff`)，导致 JSON/LangChain 序列化崩溃。系统在以下位置进行全局过滤：

| 文件 | 位置 | 过滤方式 |
|------|------|---------|
| `agent_factory.py` | `_SafeMemory.save_context()` | 存储前正则清洗 |
| `law_search.py` | `LawSearchEngine.search()` | 检索结果文本清洗 |
| `project_search_db.py` | 工具函数 | 查询结果文本清洗 |
| `jieba_bm25.py` | `JiebaBM25Retriever` | Token 清洗 |
| `main.py` | CLI 入口 | 输出前清洗 |

---

## 八、评估子系统 (eval/) **[新增]**

```
eval/
├── dataset_builder.py       # 评测数据集构建（法律/项目问答对生成）
├── generation_evaluator.py  # 生成质量评估 (LLM as Judge)
│                              ├─ 正确性 (Correctness)
│                              ├─ 相关性 (Relevancy)
│                              └─ 忠实度 (Faithfulness)
├── retrieval_evaluator.py   # 检索质量评估
│                              ├─ Recall (召回率)
│                              └─ Precision (精确率)
├── sql_evaluator.py          # NL2SQL 逻辑准确性评估
├── agent_adapter.py          # 评估用 Agent 适配器
├── llm_wrapper.py            # SimpleLLM 封装（ChatOpenAI 统一接口）
├── llm_adapter.py            # 评估 LLM 适配
├── main_evaluation.py        # 评估主流程（一键运行）
├── knowledge_base_config.yaml # 知识库评估配置
├── datasets/                 # 生成的评测数据集
└── reports/summary/          # 评估报告输出
```

**评估流程：**
1. `dataset_builder.py` — 基于知识库文档自动生成问答对
2. 各 evaluator 对检索/生成/NL2SQL 分别打分
3. LLM as Judge 使用独立评估模型（可不同于生产模型）
4. 生成 Markdown 对比报告到 `reports/summary/`

---

## 九、Docker 部署

| 容器 | 镜像 | 端口 | 说明 |
|------|------|------|------|
| `app` | 本地构建 | 8000 | FastAPI Web 应用 |

GPU 加速：使用 `docker-compose.gpu.yml` 覆盖文件，通过 `-f` 参数合并。

详细部署步骤见 [DEPLOY.md](DEPLOY.md)。

---

## 十、目录结构

```
san/
├── configs/
│   ├── config.py              # 全局配置（路径、模型、检索参数，.env 环境变量加载）
│   └── prompt_config.py       # 所有 LLM 提示词集中管理（374行）
├── data/
│   ├── law/                   # 法律法规资料（JSON + PDF）
│   └── project/               # 环卫项目数据（Excel + SQLite）
├── knowledge_base/            # FAISS 向量索引（运行时生成）
├── models/                    # HuggingFace 本地模型权重
│   ├── bge-m3/
│   └── bge-reranker-v2-m3/
├── server/
│   ├── agent/
│   │   ├── agent_factory.py   # Agent 工厂（线程安全单例+_SafeMemory+流式支持）
│   │   └── tools/
│   │       ├── law_search.py         # 法律 RAG 工具（混合检索+重排序+缓存）
│   │       ├── project_search_db.py  # NL2SQL 项目查询工具
│   │       └── search_internet.py    # 智谱 Web 搜索工具
│   ├── embeddings/
│   │   └── bge_m3.py          # BGE-M3 Ollama 嵌入包装器（当前主代码使用 HuggingFace）
│   └── rag/
│       ├── data_loader.py     # 数据加载器（支持 8 种格式）
│       ├── jieba_bm25.py      # 中文 BM25 检索器（LlamaIndex BaseRetriever）
│       ├── vector_store.py    # FAISS 索引管理（含启动自动检查）
│       ├── reranker.py        # CrossEncoder 重排序
│       ├── project_db.py      # SQLite + FTS5 数据管理（多进程分词）
│       ├── project_query_engine.py  # NL2SQL 查询引擎（分类+路由+校验+执行）
│       ├── sql_validator.py   # SQL 安全校验（SQLGlot）
│       ├── vanna_setup.py     # Vanna 训练数据（222+ SQLite + 16 DuckDB 问答对）
│       └── vanna_llm.py       # Vanna LLM 适配器（OpenAI 兼容 API）
├── eval/                      # 评估子系统
│   ├── main_evaluation.py     # 评估主流程
│   ├── dataset_builder.py     # 评测数据集构建
│   ├── generation_evaluator.py # 生成质量评估 (LLM as Judge)
│   ├── retrieval_evaluator.py  # 检索质量评估
│   ├── sql_evaluator.py       # NL2SQL 评估
│   ├── agent_adapter.py       # 评估 Agent 适配
│   ├── llm_wrapper.py         # SimpleLLM (ChatOpenAI 封装，供 chat_api 和 eval 共用)
│   ├── llm_adapter.py         # 评估 LLM 适配
│   ├── knowledge_base_config.yaml
│   ├── datasets/              # 评测数据集
│   └── reports/summary/       # 评估报告
├── web/
│   ├── app.py                 # FastAPI 主应用（lifespan 初始化 + Agent 预热）
│   ├── api/
│   │   ├── upload_api.py      # 文件上传接口
│   │   ├── vectorize_api.py   # 向量化构建接口
│   │   ├── chat_api.py        # 非流式对话接口 + 统一历史管理
│   │   └── chat_streaming.py  # SSE 流式对话接口 [新增]
│   ├── templates/             # Jinja2 HTML 模板 (chat.html 含 SSE 前端)
│   └── static/                # 静态资源
├── docker/
│   ├── Dockerfile
│   ├── docker-compose.yml
│   ├── docker-compose.gpu.yml
│   ├── requirements.txt
│   └── .env.example
├── DEPLOY.md
├── README.txt
└── main.py                    # CLI 入口
```

---

## 十一、变更记录

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| 2.0 | 2026-05-15 | 新增 SSE 流式输出架构 (chat_streaming.py)；新增评估子系统 (eval/)；新增对话历史统一管理机制；修复 hybrid_retriever.py 引用错误（逻辑在 law_search.py）；更新 LLM 为 DeepSeek Chat API；嵌入模型改为 HuggingFace 本地加载；互联网搜索明确为智谱 API；新增 Unicode 代理字符过滤说明；更新模型配置表和环境变量加载方式 |
