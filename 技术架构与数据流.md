# 环卫行业智能问答系统 — 技术架构与数据流

## 一、整体架构概览

```text
┌─────────────────────────────────────────────────────────┐
│                     Web 层 (FastAPI)                      │
│  ┌──────────┐  ┌────────────┐  ┌──────────────────────┐ │
│  │ Upload   │  │ Vectorize  │  │  Chat (RAG / Agent)   │ │
│  │ API      │→ │ API        │→ │  API                  │ │
│  └──────────┘  └────────────┘  └──────────────────────┘ │
├─────────────────────────────────────────────────────────┤
│                     Agent 层 (LangChain)                  │
│  ┌──────────────────────────────────────────────────┐    │
│  │  ReAct Agent (ZERO_SHOT_REACT_DESCRIPTION)       │    │
│  │  Memory: ConversationBufferWindow (k=5)          │    │
│  │  工具: law_search / project_search_db / web      │    │
│  └──────┬──────────────┬──────────────┬─────────────┘    │
│         │              │              │                   │
│         ▼              ▼              ▼                   │
│  ┌──────────┐  ┌────────────┐  ┌──────────────┐          │
│  │ 法律 RAG │  │ NL2SQL     │  │ 互联网搜索    │          │
│  └──────────┘  └────────────┘  └──────────────┘          │
├─────────────────────────────────────────────────────────┤
│                    模型层                                  │
│  ┌────────────────┐  ┌────────────┐  ┌────────────────┐  │
│  │ BGE-M3 (嵌入)  │  │ BGE-Rerank│  │ LLM (Qwen/     │  │
│  │               │  │ -v2-m3    │  │ DeepSeek/GLM)  │  │
│  └────────────────┘  └────────────┘  └────────────────┘  │
├─────────────────────────────────────────────────────────┤
│                    数据层                                  │
│  ┌──────────┐  ┌──────────────┐  ┌──────────────────┐    │
│  │ Excel →  │  │ JSON/PDF →   │  │ FAISS 向量索引    │    │
│  │ SQLite   │  │ Document     │  │ knowledge_base/   │    │
│  │ + FTS5   │  │              │  │                   │    │
│  └──────────┘  └──────────────┘  └──────────────────┘    │
└─────────────────────────────────────────────────────────┘
```

---

## 二、数据流详解

### 流程 1：环卫项目数据 — Excel → SQLite → NL2SQL 查询

```
data/project/环卫项目数据.xlsx
        │
        ▼
  server/rag/project_db.py
        │  import_excel_to_sqlite()
        │  ① 读取 Excel (pandas)
        │  ② 列名映射（中文 → 英文列名）
        │  ③ 写入 SQLite 结构表 sanitation_projects（27 个字段）
        │  ④ jieba 分词预处理 → 写入 FTS5 全文索引表 sanitation_projects_fts
        │  ⑤ 保存 mtime+size 哈希，避免重复导入
        ▼
  data/project/sanitation_projects.db
        │
        ▼
  server/rag/project_query_engine.py  ←  ProjectQueryEngine
        │
        ├─ 启动时自动导入/更新数据
        │
        ├─ classify() ─── 查询类型分类器
        │   ├─ 规则匹配（关键词如"排名"→aggregation）
        │   └─ LLM fallback（规则无法判断时调用 LLM）
        │
        ├─ exact / fuzzy ─────────────┐
        │   ▼                         │
        │  SQLite Vanna (NL2SQL)      │
        │   ├─ DDL + 文档 + 问答对    │
        │   │  训练数据                │
        │   ├─ jieba Jaccard +        │
        │   │  Embedding 混合检索      │
        │   │  相似示例                │
        │   └─ LLM 生成 SQL           │
        │       │                     │
        │       ▼                     │
        │  SQLGlot 校验(SQLValidator)  │
        │   ├─ 语法解析                │
        │   ├─ 语句白名单(SELECT/WITH)  │
        │   └─ 操作黑名单(INSERT/DROP…) │
        │       │                     │
        │       ▼                     │
        │  SQLite 执行 → DataFrame    │
        │                             │
        ├─ aggregation ───────────────┘
        │   ▼
        │  DuckDB Vanna (NL2SQL)
        │   ├─ ATTACH SQLite 数据库
        │   ├─ 支持 strftime 等分析函数
        │   ├─ DDL + 文档 + 问答对训练
        │   └─ LLM 生成 SQL
        │       │
        │       ▼
        │  SQLGlot 校验
        │       │
        │       ▼
        │  DuckDB 执行 → DataFrame
        │
        ▼
  Agent 工具 project_search_db
        │  格式化结果（项目名称/金额/日期等）
        ▼
  LLM 生成最终回答
```

**关键文件：**
| 文件 | 作用 |
|------|------|
| `server/rag/project_db.py` | Excel 读取、SQLite 建表、FTS5 索引、数据导入 |
| `server/rag/project_query_engine.py` | 查询分类 + NL2SQL 路由 + SQL 校验 + 执行 |
| `server/rag/vanna_setup.py` | Vanna 训练数据（DDL + 文档 + 100+ 问答对） |
| `server/rag/vanna_llm.py` | Vanna LLM 适配器（OpenAI 兼容 API） |
| `server/rag/sql_validator.py` | SQLGlot 安全校验 |
| `server/agent/tools/project_search_db.py` | Agent 工具的封装与结果格式化 |

---

### 流程 2：法律法规数据 — JSON/PDF → FAISS → RAG 检索

```
data/law/
  ├─ 招采政策法规.json    ←  结构化法规（政策标题+内容+时间）
  └─ *.pdf                          ←  扫描件/文档法规
        │
        ▼
  server/rag/data_loader.py
        │  load_json_data()  /  load_law_pdf_data()
        │  JSON：提取 政策标题/内容/发布时间/施行时间/来源网站
        │  PDF：PyMuPDF 逐页提取 → 清洗非法代理字符 → 对齐 JSON 元数据格式
        ▼
  llama_index Document 列表
        │
        ▼
  SentenceSplitter              ← 分片
        │  chunk_size=800, overlap=100
        ▼
  文本节点 (TextNode) 列表
        │
        ▼
  server/rag/vector_store.py
        │  create_faiss_index()
        │  ① BGE-M3 嵌入模型 → 向量化每个节点
        │  ② FAISS IndexFlatIP (内积索引)
        │  ③ 持久化到 knowledge_base/law/
        ▼
  knowledge_base/law/  ← FAISS 索引 + 文档存储

  查询时：
  用户问题
        │
        ▼
  server/rag/hybrid_retriever.py  ← LawSearchEngine
        │
        ├─ 向量检索 (VectorIndexRetriever)  top_k=30
        │   │  BGE-M3 编码查询 → FAISS 检索
        │   ▼
        ├─ BM25 检索 (JiebaBM25Retriever)   top_k=60
        │   │  jieba 分词 → BM25s 关键词检索
        │   ▼
        ├─ Query Fusion (Reciprocal Rank Fusion)
        │   权重: [BM25=0.2, Vector=0.8]
        │   融合取 top_k=60 送入 Reranker
        │   ▼
        ├─ BGE-Reranker-v2-m3 (CrossEncoder)
        │   对 60 个候选逐对(query, doc)打分
        │   重排序 → 输出 top_n=5
        │   ▼
        └─ 缓存 (LRU, 相同 query 复用)
              │
              ▼
  Agent 工具 law_search
        │  格式化（含元数据：标题/发布时间/来源/内容）
        ▼
  LLM 生成最终回答
```

**关键文件：**
| 文件 | 作用 |
|------|------|
| `server/rag/data_loader.py` | 多格式数据解析（Excel/JSON/PDF/CSV/TXT/DOCX/DOC） |
| `server/rag/vector_store.py` | FAISS 索引构建、加载、持久化（含 PDF 文件批量处理） |
| `server/rag/jieba_bm25.py` | 中文 BM25 检索器（jieba 分词 + bm25s） |
| `server/rag/reranker.py` | BGE-Reranker-v2-m3 CrossEncoder 重排序 |
| `server/agent/tools/law_search.py` | LawSearchEngine 封装（混合检索+重排序+缓存） |
| `server/embeddings/bge_m3.py` | BGE-M3 嵌入模型加载 |

---

### 流程 3：文件上传 → 向量化 → 自定义知识库

```
用户上传文件 (Excel/PDF/JSON等)
        │
        ▼
  POST /api/upload  ← upload_api.py
        │  ① 校验扩展名白名单
        │  ② 保存到 data/ 目录（同名自动编号）
        ▼
  data/ 目录中已有文件
        │
        ▼
  POST /api/vectorize  ← vectorize_api.py
        │  { file_name, kb_name, chunk_size, chunk_overlap }
        ▼
  server/rag/vector_store.py :: build_index_from_file()
        │
        ├─ 1. DataLoader.load_file()  → 自动识别扩展名
        │   ├─ .xlsx/.xls → load_excel_data()
        │   ├─ .json      → load_json_data()
        │   ├─ .pdf       → load_pdf_data()
        │   ├─ .csv       → load_csv_data()
        │   ├─ .txt       → load_txt_data()
        │   ├─ .docx      → load_docx_data()
        │   └─ .doc       → load_doc_data()
        │
        ├─ 2. SentenceSplitter 分片
        │
        ├─ 3. BGE-M3 向量化
        │
        └─ 4. FAISS 索引 → 持久化到 knowledge_base/<kb_name>/
                      │
                      ▼
  知识库列表 GET /api/knowledge-bases
        对话时加载对应索引进行检索
```

**关键文件：**
| 文件 | 作用 |
|------|------|
| `web/api/upload_api.py` | 文件上传接口，白名单校验，防重名 |
| `web/api/vectorize_api.py` | 向量化构建接口，知识库管理 |
| `server/rag/vector_store.py` | 核心索引构建逻辑 |

---

### 流程 4：对话查询（Agent 自动路由）

```
用户提问
        │
        ▼
  POST /api/chat  ← chat_api.py
        │  { query, kb_name }
        │
        ├─ kb_name="auto" ─────────────────────── Agent 模式
        │   │
        │   ▼
        │  server/agent/agent_factory.py
        │   │  init_agent()  ← 单例 + 线程安全
        │   │
        │   ├─ 1. 加载法律知识库 (FAISS index)
        │   │
        │   ├─ 2. 创建三个工具：
        │   │   ├─ law_search(query)         → 法律 RAG 检索
        │   │   ├─ project_search_db(query)  → NL2SQL 查询
        │   │   └─ search_internet(query)    → 智谱Web搜索API
        │   │
        │   ├─ 3. LLM (Qwen/DeepSeek) 作为 ReAct Agent 大脑
        │   │
        │   ├─ 4. ReAct 循环：
        │   │   Thought → Action → Observation → Thought → ... → Final Answer
        │   │   （最多 5 轮迭代，针对同一方向最多 2 次调用）
        │   │
        │   └─ 5. 返回 { answer, thinking_steps, tool_calls_count }
        │
        ├─ kb_name="law" ──────────────────── 法律单库模式
        │   │
        │   ▼
        │  LawSearchEngine.search(query)
        │   → 混合检索 + Reranker → 结果作为 context
        │   → LLM 基于 context 生成回答
        │
        ├─ kb_name="project" ──────────────── 项目单库模式
        │   │
        │   ▼
        │  ProjectQueryEngine.query(query)
        │   → NL2SQL 生成 + 执行 → 结果格式化作为 context
        │   → LLM 基于 context 生成回答
        │
        └─ 其他 kb_name ──────────────────── 自定义知识库
            │
            ▼
          通用检索引擎
           → 尝试混合检索 (Vector + BM25)，退化到纯向量检索
           → 结果作为 context → LLM 生成回答
```

---

## 三、关键模型配置

| 模型 | 用途 | 加载方式 | 维度 |
|------|------|---------|------|
| BGE-M3 | 文本嵌入/向量化 | HuggingFace/Ollama | 1024 |
| BGE-Reranker-v2-m3 | 检索结果重排序 | HuggingFace CrossEncoder | - |
| Qwen2.5:7b / DeepSeek / GLM-4 | LLM 对话生成 | Ollama API / OpenAI 兼容 API | - |

---

## 四、核心数据结构

### SQLite 项目表 (`sanitation_projects`)

27 个字段，涵盖：
- **地理信息**：大区、省份、城市、区县、城乡分类
- **项目信息**：项目名称、项目类型、项目状态、是否PPP
- **参与方**：招标单位、招标代理、采购主体、中标公司
- **财务**：年化金额、合同金额、年限
- **时间**：中标时间、时间节点、预计合同到期时间、剩余合同期限
- **业务**：清扫面积、单价、转运补贴/处理量、转运站个数

### FTS5 全文索引

10 个文本列（jieba 分词预处理）：项目名称、招标单位、招标代理、采购主体、项目类型、项目状态、中标公司、城乡分类、备注、是否PPP

### FAISS 向量索引

- 法律知识库：`knowledge_base/law/`，IndexFlatIP，1024 维
- 自定义知识库：`knowledge_base/<kb_name>/`

---

## 五、检索漏斗（法律知识库）

```text
原始文档 (JSON + PDF)
   │
   ▼
SentenceSplitter (800/100)
   │
   ▼
┌─────────────────┐
│ 向量检索 top 30  │     BM25 检索 top 60 │
│ (BGE-M3 + FAISS)│     (jieba + bm25s)  │
└────────┬────────┘     └────────┬─────────┘
         │                       │
         └──────────┬────────────┘
                    │
                    ▼
         Query Fusion (权重 0.8 + 0.2)
                    │
                    ▼
             候选集 top 60
                    │
                    ▼
         BGE-Reranker-v2-m3 (CrossEncoder)
                    │
                    ▼
             精排输出 top 5
                    │
                    ▼
              LLM 生成回答
```

---

## 六、NL2SQL 查询漏斗（项目库）

```text
  用户自然语言问题
        │
        ▼
  ┌─────┴──────┐
  │ 查询分类器   │ ← 规则匹配 + LLM fallback
  └─────┬──────┘
        │
  ┌─────┴──────┬──────────┐
  │ exact/fuzzy│aggregation│
  └─────┬──────┴──────────┘
        │
        ▼
  Vanna NL2SQL (LLM 生成 SQL)
        │  ① 相似问答对检索 (Jaccard + Embedding)
        │  ② 相关 DDL + 文档注入
        │  ③ LLM 合成 SQL
        ▼
  SQLGlot 校验
        │  (语法+白名单+黑名单)
        ▼
  执行引擎 (SQLite / DuckDB)
        │
        ▼
  结果格式化 → Agent 返回 → LLM 最终回答
```

---

## 七、Docker 部署

| 容器 | 镜像 | 端口 | 说明 |
|------|------|------|------|
| `ollama` | `ollama/ollama:latest` | 11434 | 运行对话模型（根据需求选择） |
| `app` | 本地构建 | 8000 | FastAPI Web 应用 |

GPU 加速：使用 `docker-compose.gpu.yml` 覆盖文件，通过 `-f` 参数合并。

详细部署步骤见 [DEPLOY.md](DEPLOY.md)。

---

## 八、目录结构

```
san/
├── configs/
│   ├── config.py              # 全局配置（路径、模型、检索参数）
│   └── prompt_config.py       # 所有 LLM 提示词集中管理
├── data/
│   ├── law/                   # 法律法规资料（JSON + PDF）
│   └── project/               # 环卫项目数据（Excel + SQLite）
├── knowledge_base/            # FAISS 向量索引（运行时生成）
├── models/                    # HuggingFace 本地模型权重
│   ├── bge-m3/
│   └── bge-reranker-v2-m3/
├── server/
│   ├── agent/
│   │   ├── agent_factory.py   # Agent 工厂（单例+线程安全）
│   │   └── tools/
│   │       ├── law_search.py         # 法律 RAG 工具
│   │       ├── project_search_db.py  # NL2SQL 项目查询工具
│   │       └── search_internet.py    # 互联网搜索工具
│   ├── embeddings/
│   │   └── bge_m3.py          # 嵌入模型
│   └── rag/
│       ├── data_loader.py     # 数据加载器（支持 8 种格式）
│       ├── jieba_bm25.py      # 中文 BM25 检索器
│       ├── vector_store.py    # FAISS 索引管理
│       ├── reranker.py        # CrossEncoder 重排序
│       ├── project_db.py      # SQLite + FTS5 数据管理
│       ├── project_query_engine.py  # NL2SQL 查询引擎
│       ├── sql_validator.py   # SQL 安全校验
│       ├── vanna_setup.py     # Vanna 训练数据
│       └── vanna_llm.py       # Vanna LLM 适配器
├── web/
│   ├── app.py                 # FastAPI 主应用
│   ├── api/
│   │   ├── upload_api.py      # 文件上传接口
│   │   ├── vectorize_api.py   # 向量化构建接口
│   │   └── chat_api.py        # 对话接口
│   ├── templates/             # Jinja2 HTML 模板
│   └── static/                # 静态资源
├── docker/
│   ├── Dockerfile
│   ├── docker-compose.yml
│   ├── docker-compose.gpu.yml
│   └── .env.example
├── DEPLOY.md
└── main.py                    # CLI 入口
```
